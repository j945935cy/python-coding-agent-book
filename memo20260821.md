# Python Coding Agent Book 進度備忘錄

日期：2026-08-21  
專案路徑：本檔所在 Repository 根目錄
Repository：`https://github.com/j945935cy/python-coding-agent-book`  
分支：`main`  
第 6～8 章擴寫前基準 Commit：`21402e4b5c2048e15d3a35daf1a31206996374e7`

## 一、專案目標

製作一本給 Python 開發者的繁體中文實作書，從零建立可測試、可觀察、可停止且有安全邊界的迷你 Coding Agent。

書稿採台灣用語，不把模型能力寫成魔法；教學主軸是：

```text
Message
→ AgentContext
→ ModelClient
→ ToolCall
→ Validation／Safety
→ ToolRegistry
→ ToolResultMessage
→ Agent Loop 下一回合
```

出版目標包含 Google Play Books 相容 EPUB、`zh-TW` metadata、靜態 SVG 圖解、完整目錄、封面與上架驗證。目前仍在正文擴寫階段，不能標記為可出版成品。

## 二、目前已完成

### 1. 技術原型

核心套件位於 `src/mini_agent/`，已具備：

- Message 資料模型
- `AgentContext`
- `ModelClient`／`FakeModel`
- `AgentConfig`
- `run_agent_loop()`
- `AgentTool`／`ToolRegistry`
- Calculator、Read、Write、Edit、Bash 工具
- ToolCall 驗證
- Workspace 路徑限制
- 同步／非同步 Safety Hook
- 循序／平行工具執行
- 事件系統
- 合作式取消
- 工具逾時
- 最大回合數
- 錯誤結果回填

### 2. V0～V10 可執行版本

`examples/` 目前有 11 個版本：

| 版本 | 檔案 | 主題 |
|---|---|---|
| V0 | `v00_chatbot_baseline.py` | 單次模型回應 |
| V1 | `v01_fake_model_loop.py` | FakeModel Agent Loop |
| V2 | `v02_workspace_tools.py` | Workspace 檔案工具 |
| V3 | `v03_agent_file_loop.py` | Agent 與檔案工具整合 |
| V4 | `v04_events_parallel_cancel.py` | 事件、平行與取消基礎 |
| V5 | `v05_error_recovery.py` | 錯誤恢復與最大回合數 |
| V6 | `v06_event_consumer.py` | 事件消費端 |
| V7 | `v07_parallel_order.py` | 平行工具與穩定結果順序 |
| V8 | `v08_cooperative_cancel.py` | 合作式取消 |
| V9 | `v09_safety_policy.py` | Safety Hook |
| V10 | `v10_complete_agent.py` | 完整整合版 |

能力差異表：`reports/version-matrix.md`

### 3. 自動驗證

統一命令：

```bash
uv run --extra test python scripts/verify_all.py .
```

目前實際結果：

```text
[PASS] chapter structure
[PASS] Python code blocks
[PASS] example outputs
[PASS] API references
[PASS] Traditional Chinese style
[PASS] learning sections
[PASS] pytest
[PASS] compileall
valid=True
```

目前量化結果：

- pytest：40 passed
- 章節：18 章，編號 1～18 完整
- Python 程式碼區塊：49 blocks，0 syntax errors
- V0～V10：全部 returncode=0 且輸出符合 manifest
- 術語稽核：0 violations
- API 參考：無缺失符號
- 每章都有非空「練習」與「本章驗收」

驗證相關檔案：

- `scripts/verify_all.py`
- `scripts/verify_examples.py`
- `scripts/audit_chapters.py`
- `scripts/audit_code_blocks.py`
- `scripts/audit_api_references.py`
- `scripts/audit_style.py`
- `scripts/audit_learning_sections.py`
- `reports/test-report.md`

### 4. 書稿狀態

18 章 Markdown 都已建立：`manuscript/chapters/01-*.md` 到 `18-*.md`。

第一輪出版篇幅擴寫已完成：

- 第 1 章：聊天機器人與 Agent
- 第 2 章：七個 Agent 模組
- 第 3 章：訊息資料模型
- 第 4 章：Agent Context
- 第 5 章：Config、Hook 與依賴注入
- 第 6 章：安全 Calculator、未知操作與數值型態錯誤
- 第 7 章：AgentTool Protocol、Registry 註冊與分派
- 第 8 章：ToolCall Validation、Safety Hook 與錯誤分類

第 1～8 章已完成第一輪擴寫。第 9～18 章仍是不同深度的初稿；其中第 11、13、14、15、17、18 章已做過第一輪局部補強，但尚未達到第 1～8 章目前的完整度。

章節狀態表：`reports/chapter-status.md`

### 5. EPUB 靜態 SVG

已完成 6 張圖：

- `manuscript/assets/agent-loop.svg`
- `manuscript/assets/seven-modules.svg`
- `manuscript/assets/message-pairing.svg`
- `manuscript/assets/context-lifecycle.svg`
- `manuscript/assets/dependency-injection.svg`
- `manuscript/assets/tool-contract-pipeline.svg`

目前圖解均：

- XML 可解析
- 不含 script
- 使用 `viewBox`
- 有 `<title>`、`<desc>`、`role="img"` 與 `aria-labelledby`
- 正文有替代文字與文字摘要
- 不依賴 JavaScript 或 Mermaid 執行環境

完整圖解規劃：`reports/illustration-plan.md`

### 6. 出版管理文件

- `reports/project-scan.md`
- `reports/chapter-status.md`
- `reports/test-report.md`
- `reports/difficulty-review.md`
- `reports/version-matrix.md`
- `reports/illustration-plan.md`
- `reports/prepublication-checklist.md`

## 三、重要寫作與技術決策

### 寫作語言

- 使用繁體中文與台灣慣用詞。
- 使用「程式、函式、設定、使用者、資訊、目錄、檔案、物件、非同步、執行緒」。
- 避免簡體與中國用語，例如「程序、函数、配置、用户、信息、目录、文件、对象、异步、线程」。
- 語氣直接、實務導向，不使用空泛宣傳語。

### 章節結構

出版擴寫章節預設包含：

1. 本章目標
2. 真實問題或失敗情境
3. 核心方法與責任邊界
4. 可執行程式或預期輸出
5. 失敗分類與安全停止方式
6. 圖解與文字摘要（只在能降低理解負擔時加入）
7. 檢查清單
8. 三題分級練習
9. 可驗收條件

### 技術原則

- 核心測試不依賴付費模型或 API Key。
- 不使用 `eval()` 執行模型輸入。
- Workspace 邊界由程式碼強制，不依賴 system prompt。
- Agent Loop 不知道供應商 SDK 細節。
- ToolCall 與 ToolResult 使用 call ID 配對。
- Safety Hook 位於工具副作用之前。
- 平行模式只用於互不相依工具。
- Context 目前只有 messages 會由 `convert_to_llm()` 序列化；system prompt 與 metadata 不會自動送出。
- `AgentContext.copy()` 是容器淺拷貝，不是深拷貝。

### 圖解原則

只在以下情況加圖：

- 資料流
- 模組責任
- 狀態轉換
- 安全攔截
- 循序／平行／取消時間關係

正式 EPUB 使用預先產生的靜態 SVG，正文保留替代文字與文字摘要。

## 四、下一批工作：直接從這裡接續

### 已完成批次：第 6～8 章

- 完成安全 Calculator、`AgentTool`／Registry 與 ToolCall Validation 第一輪出版篇幅擴寫。
- 新增未知操作、數值型態、重複工具、未知工具、空或純空白 call ID，以及 Safety Hook 順序測試。
- 完成 `manuscript/assets/tool-contract-pipeline.svg`。
- 統一驗證 8 checks 全部通過；pytest 為 40 passed。

### P0：擴寫第 9～12 章

目標是建立有一致 Workspace 邊界的檔案與 Shell 工具管線：

```text
Workspace 路徑解析
→ ReadTool
→ WriteTool
→ EditTool
→ 受限 BashTool
→ 逾時／錯誤結果
```

建議順序：

1. 讀取並核對：
   - `manuscript/chapters/09-read-tool.md`
   - `manuscript/chapters/10-write-tool.md`
   - `manuscript/chapters/11-edit-tool.md`
   - `manuscript/chapters/12-bash-tool.md`
   - `src/mini_agent/safety.py`
   - `src/mini_agent/tools/file_tools.py`
   - `src/mini_agent/tools/bash_tool.py`
   - `tests/test_safety.py`
   - `tests/test_tools.py`
   - `tests/test_bash_tool.py`
2. 先建立四章共用的 Workspace 邊界說明，避免重複警告。
3. 第 9 章補 Read 的路徑解析、文字讀取、Workspace 逃逸與不存在檔案案例。
4. 第 10 章補 Write 的父目錄處理、覆寫語意與副作用前檢查。
5. 第 11 章補唯一匹配 Edit、零匹配、多匹配與檔案狀態轉換。
6. 第 12 章補受限命令、危險語法拒絕、逾時與子行程停止。
7. 製作 Workspace 邊界與檔案狀態轉換 SVG。
8. 每章加入可複製程式、預期輸出、失敗案例、檢查清單、練習與驗收。
9. 執行統一驗證並更新所有報告與 memo。

### P1：後續章節批次

- 第 13～16 章：Agent Loop、事件、平行、取消；製作 Loop 狀態機與時間軸 SVG。
- 第 17～18 章：安全政策、Context 預算、V10 完整整合；製作安全分層與完整架構 SVG。

### P2：出版生產

正文擴寫完成後才進入：

- 練習解答與參考實作
- 第一輪技術編輯
- 第二輪文字編輯
- EPUB 生產目錄與建置腳本
- metadata：`zh-TW`
- 作者：`Happy eBook Authors`
- 出版者：`Happy eBook`
- 封面至少 1400×2100
- EPUBCheck
- Ace by DAISY
- Google Play Books 上傳預覽

## 五、目前尚未完成，請勿誤報

- 第 9～18 章尚未全部達到出版篇幅。
- 尚未完成所有必要 SVG。
- 尚未提供完整練習解答。
- 尚未建立 EPUB 建置流程。
- 尚未完成封面、metadata、EPUBCheck、Ace 或 Google Play Books 預覽。
- 尚未連接真實模型供應商 Adapter。
- 空或只有空白字元的 ToolCall ID 會被 Validation 阻擋，但目前錯誤結果仍沿用無效 ID，不能可靠配對；正式 Adapter 必須在進入 Loop 前拒絕。
- 目前是已驗證的技術原型與部分擴寫書稿，不是可上架成品。

## 六、每批完成後固定流程

```bash
uv run --extra test python scripts/verify_all.py .
git diff --check
git add <本批檔案>
git commit -m "<清楚描述>"
git push
```

完成後確認：

```bash
git rev-parse HEAD
git ls-remote origin refs/heads/main
git status --short
```

本 memo 後續應隨重大里程碑更新，尤其是章節擴寫範圍、SVG 數量、測試數、EPUB 狀態與下一批起點。
