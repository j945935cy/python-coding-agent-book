# 專案工作規範

本專案依 `/mnt/c/Users/j945935/Downloads/HERMES_PYTHON_CODING_AGENT_BOOK.md` 執行。

## 核心規則

- 全書使用繁體中文與台灣習慣用語。
- 每階段先完成可驗收產物，再進入下一階段。
- 不一次生成 18 章正文。
- 核心測試不得依賴付費模型；使用 `FakeModel`。
- Read、Write、Edit、Bash 只能操作指定 Workspace。
- 不使用 `eval()` 執行模型內容。
- 截斷輸出不得執行工具呼叫。
- 工具執行必須有驗證、逾時、中止與錯誤處理。
- 修改前先確認現有檔案，不覆蓋使用者既有內容。

## 階段門檻

每批工作必須更新對應報告，至少記錄：範圍、檔案、測試、結果、未完成項目與風險。