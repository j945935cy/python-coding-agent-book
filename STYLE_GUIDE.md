# 寫作與程式碼風格指南

## 語言

- 使用繁體中文與台灣用語。
- 使用「物件導向」「類別」「物件」。
- 先解釋為什麼，再說明怎麼做。
- 不使用 Emoji。
- 不確定的版本、效能、價格與引用資料標示「待查證」。

## Markdown

- 無序清單使用 `-`。
- 有序步驟使用 `1. 2. 3.`。
- 程式碼區塊必須標明語言。
- 指令區塊標示執行環境，例如 `[Bash／WSL]`。
- 比較資料優先使用 Markdown 表格。

## 每章結構

情境導入 → 學習目標 → 先看執行結果 → 核心概念 → TypeScript 原始設計 → 簡單 Python → 完整程式 → 逐段解說 → 執行方式與預期結果 → 常見錯誤 → AI 輔助除錯 → 動手修改 → 章末練習 → 本章小結。

## 程式碼

- Python 3.11+。
- 核心程式優先使用標準函式庫。
- 核心迴圈不綁定單一模型供應商。
- 所有可執行範例必須能用 `FakeModel` 測試。
- 不使用 `eval()`。
- 外部路徑、Shell 指令、工具參數都必須經過驗證。